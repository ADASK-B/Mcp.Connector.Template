{
  "servers": {
    // ──────────────────────────────────────────────────────────────
    // Platform Test Application MCP Server — run via Docker container.
    // Start a verified release: docker run -p 8080:8080 ghcr.io/adask-b/mcp.connector.template@sha256:<digest>
    // Then Copilot can use the "echoSynthetic" tool directly in chat.
    // ──────────────────────────────────────────────────────────────
    "platform-test": {
      "type": "http",
      "url": "http://localhost:8080/mcp"
    }

    // ──────────────────────────────────────────────────────────────
    // Context7 — Up-to-date library documentation
    //    Fetches latest docs for any library (MCP SDK, ASP.NET, etc.)
    //    Eliminates outdated/hallucinated API references.
    //    No install needed — runs via npx.
    //    Repo: https://github.com/upstash/context7
    // ──────────────────────────────────────────────────────────────
    // "context7": {
    //   "type": "stdio",
    //   "command": "npx",
    //   "args": ["-y", "@upstash/context7-mcp@latest"]
    // }
  }
}
