replicaCount: null

# Customer value projected exclusively from the canonical resolved setup model.
# There is intentionally no chart or application default.
application:
  message: null

image:
  repository: null
  digest: null

resources: null

# Generated only by platformctl from the canonical resolved setup model.
# The chart has no fallback labels, security context, probes, Secret wiring or
# ServiceAccount semantics outside this fixed interface.
platformRuntime: null
